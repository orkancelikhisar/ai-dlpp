{
  "positives": [
    "The app is failing against postgresql://svc_ledger@db-prod-01.internal:5432/ledger",
    "Reporting still points at mysql://reporting@mysql-rr-03.internal:3306/warehouse",
    "mongodb+srv://cluster1.internal/kycdocs?retryWrites=true was in the config map.",
    "connection_string: mssql://svc_recon@sql-prod-02.internal:1433/recon",
    "redis://cache-02.internal:6379/3 is timing out during the peak window.",
    "The publisher uses amqp://payments@rabbit-01.internal:5672/settlements",
    "jdbc:postgresql://db-prod-04.internal:5432/collections is in the datasource bean.",
    "datasource_url=jdbc:mysql://mysql-01.internal:3306/audit",
    "postgres://readonly@10.42.7.19:5432/customer360 was pasted into the ticket.",
    "Use mongodb://mongo-01.internal:27017/statements for the nightly export.",
    "The DSN is postgresql://etl_user@db-analytics.internal:5432/marts",
    "Server=tcp:sql-prod-02.internal,1433;Database=recon;Encrypt=true",
    "host=db-prod-01.internal port=5432 dbname=ledger user=svc_ledger",
    "redis://sessions-01.internal:6379/0 appears in the incident notes.",
    "mysql://svc_billing@mysql-02.internal:3306/billing?sslMode=REQUIRED",
    "The staging string mongodb+srv://cluster0-staging.internal/kyc leaked into the ticket.",
    "jdbc:oracle:thin:@ora-prod-01.internal:1521/FINPRD is the legacy datasource.",
    "db_url: postgres://svc_alerts@db-prod-03.internal:5432/alerts",
    "amqps://events@rabbit-02.internal:5671/audit is the TLS listener.",
    "Failover target is postgresql://svc_recon@db-dr-01.internal:5432/recon_replica"
  ],
  "negatives": [
    "The runbook lives at https://intranet.example.com/runbooks/db-restore",
    "The production database is down and the incident bridge is open.",
    "Connect with psql -h db-prod-01.internal -U svc_ledger -d ledger",
    "The export landed at s3://finops-exports/2026/04/ledger.parquet",
    "Bootstrap servers are kafka-01.internal:9092,kafka-02.internal:9092",
    "The schema name is ledger_v2 and the owner is svc_ledger.",
    "Hop through the bastion with ssh db-prod-01.internal first.",
    "http://localhost:5432 is not reachable from the laptop.",
    "The connection string was removed from the ticket before it was shared.",
    "ldap://directory.internal:389 is the corporate directory endpoint.",
    "Check file:///var/lib/postgresql/data/pg_hba.conf on the primary.",
    "The datasource bean is named primaryDataSource in the Spring config.",
    "The mongo shell takes the host and port on the command line.",
    "JDBC drivers are packaged inside the fat jar we ship.",
    "The DSN was rotated after last quarter's incident.",
    "smtp://mail-relay.internal:25 handles outbound notifications.",
    "db-prod-01.internal resolves to 10.42.7.19 from the bastion.",
    "PostgreSQL is on version 16 in production and 15 in staging.",
    "The runbook says to fail over to the replica in the DR region.",
    "AMQP is the protocol; the broker itself is rabbit-01.internal."
  ]
}
